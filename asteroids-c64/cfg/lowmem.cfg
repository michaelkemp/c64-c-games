# Same as hires-bounce/cfg/lowmem.cfg, plus one change: the ZP segment
# is grown from cc65's stock 26 bytes ($0002-$001B) to 96 bytes
# ($0002-$0061), to fit src/drawline_asm.s's ~30 bytes of hand-placed
# zero-page state (ptr/mask/err/e2/dx/dy/...) alongside cc65's own
# runtime-reserved zp pointers. $0002-$0061 stays well clear of the
# $90+ zero-page bytes the KERNAL/BASIC use, so this is safe even though
# we don't disable interrupts for the whole program (only around each
# timed benchmark section).
#
# Same as cc65's stock c64.cfg otherwise, except __HIMEM__ is lowered
# from $D000 to $3F00 (was $4000 -- see below for the extra $100). See
# README.md's "Why a custom linker config" for the reason: the stock
# config lets cc65 place code/data anywhere from $0801 up to $D000,
# which would happily overlap the $4000-$7FFF window this program POKEs
# VIC bank 1's screen matrix and bitmap into by fixed address. Capping
# __HIMEM__ keeps every cc65-managed byte (code, data, the software
# stack) below that window, so nothing the linker places can ever
# collide with memory the VIC-II is reading as graphics.
#
# __HIMEM__ was $3F00, not $4000, in the version of this file copied
# from vector-scene/ -- carving out SCRATCH ($3F00-$3FFF) as a fixed,
# genuinely-reserved 256 bytes for benchmarks to POKE automated results
# into (tests/bench_report.h) for tools/bench.sh's VICE-remote-monitor
# readback, instead of a bare "$2000 is probably empty" guess. That
# guess was wrong at least once over in vector-scene/: a benchmark
# linking two full asm modules plus a 720-byte table had its own
# compiled code+rodata+data run right through the address being used
# as "scratch", corrupting the running program and producing
# intermittent, build-dependent-looking "measurement anomalies" that
# looked like real bugs in the code being measured until traced back
# to this.
#
# asteroids-c64 doesn't have that benchmark yet -- nothing built here
# (main.c, doublebuf.c, keytest.c) includes bench_report.h or touches
# $3F00-$3FFF, confirmed with a repo-wide grep before making this
# change. Reserving 256 bytes against a hazard that doesn't exist here
# yet was costing main.c's line-draw panic screen (src/main.c's
# panic_bad_line()) real room it needs for a full diagnostic dump, so
# __HIMEM__ is back to $4000 (still capped below VIC bank 1's
# $4000-$7FFF window -- see below) and SCRATCH is gone. If a benchmark
# here ever does need it, reintroducing the carve-out is a one-line
# change, and ld65's overflow check will immediately say so if
# whatever's being linked at the time doesn't fit around it.

FEATURES {
    STARTADDRESS: default = $0801;
}
SYMBOLS {
    __LOADADDR__:  type = import;
    __EXEHDR__:    type = import;
    __STACKSIZE__: type = weak, value = $0800; # 2k stack
    __HIMEM__:     type = weak, value = $4000; # was $D000 in the stock cfg
}
MEMORY {
    ZP:       file = "", define = yes, start = $0002,           size = $0060;
    LOADADDR: file = %O,               start = %S - 2,          size = $0002;
    HEADER:   file = %O, define = yes, start = %S,              size = $000D;
    MAIN:     file = %O, define = yes, start = __HEADER_LAST__, size = __HIMEM__ - __HEADER_LAST__;
    BSS:      file = "",               start = __ONCE_RUN__,    size = __HIMEM__ - __STACKSIZE__ - __ONCE_RUN__;
}
SEGMENTS {
    ZEROPAGE: load = ZP,       type = zp;
    LOADADDR: load = LOADADDR, type = ro;
    EXEHDR:   load = HEADER,   type = ro;
    STARTUP:  load = MAIN,     type = ro;
    LOWCODE:  load = MAIN,     type = ro,  optional = yes;
    CODE:     load = MAIN,     type = ro;
    RODATA:   load = MAIN,     type = ro;
    DATA:     load = MAIN,     type = rw;
    INIT:     load = MAIN,     type = rw;
    ONCE:     load = MAIN,     type = ro,  define   = yes;
    BSS:      load = BSS,      type = bss, define   = yes;
}
FEATURES {
    CONDES: type    = constructor,
            label   = __CONSTRUCTOR_TABLE__,
            count   = __CONSTRUCTOR_COUNT__,
            segment = ONCE;
    CONDES: type    = destructor,
            label   = __DESTRUCTOR_TABLE__,
            count   = __DESTRUCTOR_COUNT__,
            segment = RODATA;
    CONDES: type    = interruptor,
            label   = __INTERRUPTOR_TABLE__,
            count   = __INTERRUPTOR_COUNT__,
            segment = RODATA,
            import  = __CALLIRQ__;
}
